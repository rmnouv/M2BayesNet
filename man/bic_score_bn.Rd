% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommonFunction.R, R/naive.r
\name{bic_score_bn}
\alias{bic_score_bn}
\title{Global BIC score for a Bayesian network}
\usage{
bic_score_bn(adj, X, distribution = c("gaussian", "multinomial"))

bic_score_bn(adj, X, distribution = c("gaussian", "multinomial"))
}
\arguments{
\item{adj}{Integer or numeric p x p adjacency matrix representing the DAG.
`adj[i, j] = 1` means there is a directed edge i -> j.}

\item{X}{Data frame or matrix of size N x p containing the data.
Must be numeric for \code{distribution = "gaussian"} and
categorical (or coercible to factors) for \code{distribution = "multinomial"}.}

\item{distribution}{Character string, either `"gaussian"` or `"multinomial"`,
selecting the type of local likelihood model used in the BIC score.}
}
\value{
Numeric, global BIC score (sum of local scores) for the DAG
  encoded by \code{adj}. Returns \code{-Inf} if \code{adj} is not acyclic.

Numeric, global BIC score (sum of local scores) for the DAG
  encoded by \code{adj}. Returns \code{-Inf} if \code{adj} is not acyclic.
}
\description{
Global BIC score for a Bayesian network

Global BIC score for a Bayesian network
}
